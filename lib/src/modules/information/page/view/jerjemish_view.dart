import 'package:flutter/material.dart';

class JerjemishView extends StatelessWidget {
  const JerjemishView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
